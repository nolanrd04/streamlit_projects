# Project3

Run:

```
Steps to run:
1. (OPTIONAL) Download the https://www.kaggle.com/datasets/prasunroy/natural-images data set and extract 3 folders: airplaine, car, and motorbike.
2. (OPTIONAL) Put the folders in the same directory as dataProcessor.py
3. (OPTIONAL) Convert images to grayscaled and resized np arrays by running dataProcessor.py
4. Run CNN.py to view model training and save local model.
5. Run the following command to test the model:
streamlit run app.py --server.port 8603
```
